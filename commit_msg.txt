Remove old admin web pages not in spec (clean up legacy platform code)

The backend web app had old admin/management HTML pages from a generic
community health platform that are not required by the MCH VoiceCare
implementation spec. The API endpoints for these apps remain — only the
server-rendered HTML views were removed.

Removed web pages (4 apps, 11 templates):
- Communication: campaign list, detail, form (old platform feature)
- Integrations: list, form (old platform feature)
- Reports: list, detail, generate (old report builder; spec §27
  uses aggregate dashboards, not a report builder)
- Organisations: list, form, delete (old org CRUD; spec §21.1
  defines hierarchy as data, managed via Django admin)

Cleaned up:
- config/web_urls.py: removed 4 old URL includes
- templates/partials/sidebar.html: removed 4 old sidebar links
  (Organisation Units, Reports, Communication, Integrations)
- Deleted templates/communication/, templates/integrations/,
  templates/reports/, templates/organisations/

Remaining web pages (all spec-required per §6.2, §27):
- Dashboard, Login, Logout
- Aggregate Dashboard (§27.1)
- Monitoring (§27.2 clinical safety)
- Override Log (§3.1, §23)
- Packages (§24 package management)
- Configuration (§33 config that must not be hard-coded)
- Audit Log (§23)
- Users & Roles (§21 access control)
- Clinical supervisor views: Clients, Pregnancy, Newborn,
  Immunisation, Growth, Referrals, Notifications

API endpoints unchanged — all spec-required APIs remain:
- /api/v1/communication/telephony/webhook/ (§17)
- /api/v1/organisations/ (§21, §32)
- /api/v1/reports/ (§27)
- /api/v1/integrations/ (§20.2)

All 664 backend tests pass.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
