Add native security, API docs, Docker dev, integration tests, TTS fallback

Five quality improvements to close remaining code-doable gaps:

## 1. Android Native Security Module (spec §22.2)
- ScreenSecurityModule.kt: native module exposing setFlagSecure + isFlagSecureSet
- ScreenSecurityPackage.kt: React Native package registration
- Registered in MainApplication.kt
- ScreenSecurity.ts: async + sync API, isFlagSecureSet query
- 34 security tests (ScreenSecurity + NotificationSanitizer)

## 2. API Documentation via drf-spectacular (spec §29)
- OpenAPI 3 schema at /api/schema/
- Swagger UI at /api/schema/swagger/
- ReDoc at /api/schema/redoc/
- 34 @extend_schema(tags=[...]) decorators on all viewsets
- SPECTACULAR_SETTINGS with 14 domain tags, JWT security scheme
- Static schema file generated (349 KB)
- 10 API docs tests

## 3. Docker Compose Dev Environment (spec §29)
- docker-compose.yml: PostgreSQL 16 + Redis 7 + Django backend
- Dockerfile.dev with ipython, django-debug-toolbar
- .dockerignore, .env.example
- 13 Docker config tests (YAML validation, service checks)
- README updated with Docker Compose instructions

## 4. Integration Tests (spec §3-§18)
- 8 workflow test classes covering:
  - Pregnancy danger-sign → referral → closed-loop
  - Patient registration → pregnancy episode → ANC observations
  - OCR scan → field extraction → human confirmation
  - Newborn assessment → danger signs → referral
  - Immunisation → CWC → vaccine dose
  - Audit trail integrity
  - Sync/config/health endpoints
  - Bulk patient operations
- 15 integration tests

## 5. TTS Audio Fallback (spec §17.2)
- TTSFallbackManager: pyttsx3 → gTTS → silent placeholder chain
- TTS text for all 15 required IVR prompts
- generate_tts_fallbacks management command (--dry-run, --language)
- AudioAsset records marked with recorded_by="tts_fallback"
- ensure_fallback_for_prompt skips when professional recording exists
- ensure_all_fallbacks processes all required prompts
- 18 TTS fallback tests

## Verification
- Backend: 871 tests pass (was 815, +56 new)
- Mobile: 258 tests pass (was 224, +34 new)
- Total: 1129 tests pass
- APK builds successfully (32 MB)

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
