Remove 26 old admin screens not in spec (clean up legacy platform code)

The mobile app was built on top of a generic community health platform
that included admin/management screens not required by the MCH VoiceCare
implementation spec. These belong in the backend Django admin, not the
mobile facility app.

Deleted screens (26):
- Campaigns: CampaignList, CampaignDetail, CampaignForm
- Templates: TemplateList, TemplateForm, TemplateDetail
- Households: HouseholdList, HouseholdForm
- Integrations: IntegrationList, IntegrationForm
- OrgUnits: OrgUnitList, OrgUnitForm, OrgUnitDelete
- Users: UserList, UserForm, RoleScopeAssign
- Reports: ReportList, ReportDetail, ReportGenerate, ScheduledReportForm
- Profiles: ProfileList, ProfileDetail, ProfileGenerate
- Capabilities: CapabilityList, CapabilityForm
- Monitoring: MonitoringScreen

Cleaned up:
- navigation/types.ts: removed 26 old route types
- App.tsx: removed 26 old imports and Stack.Screen registrations
- DashboardScreen: removed old admin entries from supportModules,
  removed Profiling module (was referencing deleted ProfileList)
- ReferralListScreen: removed "Capabilities" button (was navigating
  to deleted CapabilityList)

Remaining screens (41) are all spec-required clinical features:
- Login, Dashboard, SyncStatus, AuditList
- Person (list, detail, form)
- Pregnancy (list, register, detail, observe, assessment, close, transfer)
- Newborn (list, register, detail, observe, close, transfer)
- Immunisation (list, register, child detail, record dose)
- Defaulter (list, detail, trace), CWC (session, detail)
- Growth (list, detail, record, chart)
- OCR (scan, confirm)
- Referral (list, detail, create, QR slip)
- TaskList, NotificationDetail
- ClinicianOverride, VoiceRecord

All 202 tests pass.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
