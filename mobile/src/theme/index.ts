/**
 * Theme barrel — import design tokens from here.
 *
 *   import {space, radius, type, elevation, lightColors} from '../theme';
 *
 * UX-001: these tokens are the single source of truth for the mobile visual
 * language and are mirrored in the web Tailwind theme.
 */
export * from './colors';
export * from './tokens';
export * from './useTheme';
