<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Production: enforce TLS 1.2+ for all external traffic (spec §22.3) -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Production API domain — TLS enforced, no certificate pinning.
         Railway uses cloud infrastructure that rotates certificates, and
         with only a single pin (no backup), any rotation breaks all API
         access on physical devices. Standard certificate chain validation
         via the system trust store is sufficient for transport security.
         To re-enable pinning: obtain the current SPKI hash, add it as a
         backup pin first, deploy, then add the primary pin. -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="false">web-production-a4e4b.up.railway.app</domain>
    </domain-config>

    <!-- Dev only: allow cleartext to Android emulator host (10.0.2.2) -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
